export enum DbConfigType {
  LOCAL = 'LOCAL',
  REMOTE = 'REMOTE',
}

export enum ScheduleType {
  ONE_OFF = 'ONE_OFF',
  PERIODIC = 'PERIODIC',
}

export enum IntervalType {
  DAY = 'day',
  HOUR = 'hour',
  WEEK = 'week',
  MONTH = 'month',
  MINUTES = 'minutes',
}
